import 'package:flutter/material.dart';

class TimeEntryRecordScreen extends StatelessWidget {
  const TimeEntryRecordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}